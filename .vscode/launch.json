{
  // https://code.visualstudio.com/docs/editor/debugging
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "name": "Test (pnpm vitest)",
      "request": "launch",
      "command": "pnpm vitest ${relativeFile}",
      "cwd": "${workspaceRoot}"
    },
    {
      "type": "node-terminal",
      "name": "Debug (pnpm tsx)",
      "request": "launch",
      "command": "pnpm tsx ${relativeFile}",
      "cwd": "${workspaceFolder}"
    },
  ]
}
